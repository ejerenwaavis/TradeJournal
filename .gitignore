node_modules/
.env
.env.local
# Keep .env.production tracked (no secrets — only public VITE_ vars)
server/dist/
*.log
.DS_Store
# Namecheap Passenger manages this file — don't overwrite it from git
public_html/.htaccess
